# Copy to .env.local and fill in real values. Never commit .env.local.

# Base URL of the FastAPI read API (api/, M5). In local dev this is a
# separate process from `next dev` -- see README.md.
NEXT_PUBLIC_API_URL=http://localhost:8000
